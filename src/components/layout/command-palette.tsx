import { useEffect,useState } from "react";
import { useNavigate } from "@tanstack/react-router";
import { FileSearch,Files,Scale,Settings,ShieldCheck,Waypoints } from "lucide-react";
import { CommandDialog,CommandEmpty,CommandGroup,CommandInput,CommandItem,CommandList,CommandShortcut } from "@/components/ui/command";
const commands=[{label:"Analyze specification",to:"/analyze",icon:FileSearch},{label:"Search standards",to:"/standards",icon:Waypoints},{label:"Open documents",to:"/documents",icon:Files},{label:"View compliance issues",to:"/compliance",icon:ShieldCheck},{label:"Change impact",to:"/changes",icon:Scale},{label:"Settings",to:"/settings",icon:Settings}] as const;
export function CommandPalette(){const [open,setOpen]=useState(false);const navigate=useNavigate();useEffect(()=>{const handler=(event:KeyboardEvent)=>{if((event.metaKey||event.ctrlKey)&&event.key.toLowerCase()==="k"){event.preventDefault();setOpen((value)=>!value)}};window.addEventListener("keydown",handler);return()=>window.removeEventListener("keydown",handler)},[]);return <CommandDialog open={open} onOpenChange={setOpen}><CommandInput placeholder="Search pages and actions…"/><CommandList><CommandEmpty>No matching command.</CommandEmpty><CommandGroup heading="Navigate">{commands.map(({label,to,icon:Icon})=><CommandItem key={to} onSelect={()=>{setOpen(false);void navigate({to})}}><Icon/>{label}{label==="Analyze specification"&&<CommandShortcut>↵</CommandShortcut>}</CommandItem>)}</CommandGroup></CommandList></CommandDialog>}
