import { Link } from "@tanstack/react-router";
import { ArrowRight, LockKeyhole } from "lucide-react";
import { useState, type FormEvent } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";

export function AuthForm({ mode }: { mode: "login" | "signup" }) { const [done,setDone]=useState(false); const submit=(event:FormEvent)=>{event.preventDefault();setDone(true)}; const signup=mode==="signup"; return <div className="page-wrap grid min-h-[calc(100vh-7rem)] place-items-center py-12"><div className="glass-panel w-full max-w-md p-6 sm:p-8"><span className="grid size-10 place-items-center rounded-sm bg-primary text-primary-foreground"><LockKeyhole className="size-5"/></span><p className="eyebrow mt-6">{signup?"Create workspace":"Secure access"}</p><h1 className="mt-3 font-serif text-4xl text-primary">{signup?"Begin with StandardOS":"Welcome back"}</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">{signup?"Create your procurement workspace to save evidence and standards.":"Sign in to continue to your saved searches and standards."}</p>{done?<div className="mt-7 border-l-2 border-accent-foreground bg-accent/45 p-4 text-sm">Demo form submitted. Account services are not connected yet.</div>:<form onSubmit={submit} className="mt-7 grid gap-4">{signup&&<label className="grid gap-1.5 text-sm font-semibold">Full name<Input required placeholder="Your name"/></label>}<label className="grid gap-1.5 text-sm font-semibold">Work email<Input required type="email" placeholder="name@organisation.in"/></label><label className="grid gap-1.5 text-sm font-semibold">Password<Input required type="password" placeholder="••••••••"/></label><Button size="lg" className="mt-2" type="submit">{signup?"Create account":"Sign in"}<ArrowRight/></Button></form>}<p className="mt-6 text-center text-sm text-muted-foreground">{signup?"Already have access? ":"New to StandardOS? "}<Link to={signup?"/login":"/signup"} className="font-semibold text-accent-foreground underline underline-offset-4">{signup?"Sign in":"Create account"}</Link></p></div></div> }