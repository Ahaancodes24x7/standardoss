import { createFileRoute } from "@tanstack/react-router";
import { Database, RefreshCw, Upload } from "lucide-react";
import { useState } from "react";
import { PageIntro } from "@/components/app-shell";
import { Button } from "@/components/ui/button";
import { standards } from "@/lib/standards";

export const Route = createFileRoute("/admin")({ head: () => ({ meta: [{ title: "Corpus Administration — StandardOS" }, { name: "description", content: "Review and maintain the indexed Indian Standards corpus." }, { property: "og:title", content: "Corpus Administration — StandardOS" }, { property: "og:description", content: "Indexed standards and corpus maintenance tools." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: Admin });
function Admin(){const [message,setMessage]=useState("");return <div className="page-wrap pb-20 pt-8"><PageIntro eyebrow="Corpus administration" title="Indexed standards" description="Review source coverage and maintain the searchable standards collection." action={<div className="flex gap-2"><Button variant="outline" onClick={()=>setMessage("Upload queue opened — demonstration only.")}><Upload/> Upload</Button><Button onClick={()=>setMessage("Re-index request queued — demonstration only.")}><RefreshCw/> Re-index</Button></div>}/>{message&&<div className="mt-5 border-l-2 border-accent-foreground bg-accent/40 px-4 py-3 text-sm">{message}</div>}<div className="glass-panel mt-8 overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="border-b border-border bg-muted/55 text-xs uppercase tracking-[.08em] text-muted-foreground"><tr><th className="p-4">Number</th><th className="p-4">Title</th><th className="p-4">Language</th><th className="p-4">Category</th><th className="p-4">Last updated</th></tr></thead><tbody>{standards.map((standard,index)=><tr key={standard.id} className="border-b border-border last:border-0 hover:bg-accent/30"><td className="p-4 font-semibold text-primary">{standard.number}</td><td className="p-4">{standard.title}</td><td className="p-4">{standard.language}</td><td className="p-4">{standard.category}</td><td className="p-4 text-muted-foreground">{index<2?"18 Sep 2026":"02 Sep 2026"}</td></tr>)}</tbody></table></div><p className="mt-4 flex items-center gap-2 text-xs text-muted-foreground"><Database className="size-3"/> {standards.length} demonstration records indexed</p></div>}