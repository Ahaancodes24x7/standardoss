import { createFileRoute } from "@tanstack/react-router";
import { GitBranch,Search,ShieldCheck } from "lucide-react";
import { useMemo,useState } from "react";
import { standardsCatalog } from "@/data/demo";
import { Input } from "@/components/ui/input";
export const Route=createFileRoute("/_authenticated/standards")({head:()=>({meta:[{title:"Standards Explorer — STANDARDOS"},{name:"description",content:"Search Indian Standards and explore related requirements."},{property:"og:title",content:"Standards Explorer — STANDARDOS"},{property:"og:description",content:"Indian Standards relationship explorer."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Standards});
function Standards(){const [query,setQuery]=useState("");const [selected,setSelected]=useState(standardsCatalog[0]);const results=useMemo(()=>standardsCatalog.filter(s=>(s.number+s.title+s.category).toLowerCase().includes(query.toLowerCase())),[query]);return <div className="reveal"><header><p className="eyebrow">Knowledge layer</p><h1 className="page-title mt-3">Standards Explorer</h1><p className="mt-4 text-muted-foreground">Search current Indian Standards and trace connected references.</p></header><div className="relative mt-9"><Search className="absolute left-4 top-1/2 size-5 -translate-y-1/2 text-muted-foreground"/><Input value={query} onChange={e=>setQuery(e.target.value)} className="h-14 pl-12 text-base" placeholder="Search Indian Standards..."/></div><div className="mt-10 grid gap-10 lg:grid-cols-[.9fr_1.1fr]"><div className="border-t border-border">{results.map(s=><button key={s.id} onClick={()=>setSelected(s)} className={`thin-row w-full py-5 text-left ${selected?.id===s.id?"border-l-2 border-l-accent-foreground pl-4":""}`}><div className="flex justify-between gap-3"><b className="text-sm text-primary">{s.number}</b><span className="text-[10px] font-bold uppercase text-success-foreground">{s.status}</span></div><p className="mt-2 text-sm leading-6 text-muted-foreground">{s.title}</p></button>)}</div>{selected&&<aside className="glass-panel self-start p-6 lg:sticky lg:top-10"><p className="eyebrow">{selected.category} · {selected.status}</p><h2 className="mt-3 font-display text-3xl text-primary">{selected.number}</h2><p className="mt-3 leading-7 text-muted-foreground">{selected.title}</p><div className="mt-6 flex items-center gap-2 border-y border-border py-4 text-sm"><ShieldCheck className="size-4 text-accent-foreground"/><b>Certification relevance</b><span className="ml-auto">{selected.certification}</span></div><p className="eyebrow mt-7">Related Standards</p><div className="mt-5 flex items-center gap-2 overflow-x-auto pb-3"><span className="shrink-0 rounded-md border border-accent bg-background px-3 py-3 text-xs font-bold">{selected.number}</span>{selected.related.map(r=><div className="contents" key={r}><span className="graph-line min-w-6"/><span className="shrink-0 rounded-md border border-border bg-background/70 px-3 py-3 text-xs font-bold">{r}</span></div>)}</div><p className="mt-5 flex items-center gap-2 text-xs text-muted-foreground"><GitBranch className="size-4"/>Click a result to inspect its relationship map.</p></aside>}</div></div>}
