import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowRight, BookOpenCheck, Languages, Quote, SearchCheck } from "lucide-react";
import { PageIntro } from "@/components/app-shell";
import { Button } from "@/components/ui/button";

export const Route = createFileRoute("/about")({ head: () => ({ meta: [{ title: "Methodology — StandardOS" }, { name: "description", content: "How StandardOS finds and explains relevant Indian Standards." }, { property: "og:title", content: "A transparent standards methodology — StandardOS" }, { property: "og:description", content: "Plain-language guidance to hybrid retrieval, multilingual matching and citations." }, { property: "og:type", content: "article" }, { name: "twitter:card", content: "summary_large_image" }] }), component: About });
function About(){const principles=[{icon:SearchCheck,title:"We read for meaning and exact terms",text:"A tender may describe a requirement without using the title of a standard. We compare the meaning of the passage and its exact technical language, then combine those signals into one shortlist."},{icon:Languages,title:"We preserve the language of the source",text:"English and Hindi passages are mapped into a shared meaning space. Product terms, units and regulatory phrases remain available for exact matching, so translation does not erase important detail."},{icon:BookOpenCheck,title:"We ground every answer in a clause",text:"A match is only useful when a reviewer can inspect it. Each result includes the standard, section reference, quoted clause and a short explanation of why it addresses the submitted requirement."}];return <div className="page-wrap pb-20 pt-8"><PageIntro eyebrow="Methodology" title="Recommendations built to be questioned." description="StandardOS shortens the search. It does not replace professional judgement or the official standard."/><div className="mt-12 grid gap-10 lg:grid-cols-[.7fr_1.3fr]"><aside className="glass-panel h-fit p-6 lg:sticky lg:top-28"><Quote className="text-accent-foreground"/><p className="mt-5 font-serif text-2xl leading-9 text-primary">A useful answer is not only relevant. It must show where it came from.</p><p className="mt-4 text-sm leading-6 text-muted-foreground">That principle shapes every result in StandardOS.</p></aside><article className="space-y-12">{principles.map((principle)=>{const Icon=principle.icon;return <section key={principle.title} className="border-t border-border pt-8"><Icon className="size-6 text-accent-foreground"/><h2 className="mt-4 font-serif text-3xl text-primary">{principle.title}</h2><p className="mt-4 text-lg leading-8 text-muted-foreground">{principle.text}</p></section>})}<div className="glass-panel p-6"><p className="eyebrow">Responsible use</p><h2 className="mt-3 font-serif text-3xl text-primary">The official publication remains authoritative.</h2><p className="mt-4 leading-7 text-muted-foreground">Always confirm applicability, amendments and current status through BIS or the responsible authority before purchase, certification or legal reliance.</p><Button asChild className="mt-6"><Link to="/search">Try a search <ArrowRight/></Link></Button></div></article></div></div>}