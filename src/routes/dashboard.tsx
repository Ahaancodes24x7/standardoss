import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowUpRight, Bookmark, CalendarDays, Search, Sparkles } from "lucide-react";
import { PageIntro } from "@/components/app-shell";
import { Button } from "@/components/ui/button";
import { standards } from "@/lib/standards";

export const Route = createFileRoute("/dashboard")({ head: () => ({ meta: [{ title: "Procurement Dashboard — StandardOS" }, { name: "description", content: "Review saved searches, bookmarked standards and standards-discovery usage." }, { property: "og:title", content: "Procurement Dashboard — StandardOS" }, { property: "og:description", content: "Saved evidence and standards activity in one place." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: Dashboard });
function Dashboard(){const stats=[{label:"Searches this month",value:"24",detail:"+8 from August",icon:Search},{label:"Standards bookmarked",value:"12",detail:"Across 4 categories",icon:Bookmark},{label:"Top matched domain",value:"Electrical",detail:"41% of searches",icon:Sparkles}];return <div className="page-wrap pb-20 pt-8"><PageIntro eyebrow="Procurement workspace" title="Good afternoon, Ananya." description="Your recent evidence trails and bookmarked standards are ready for review." action={<Button asChild><Link to="/search"><Search/> New search</Link></Button>}/><div className="mt-8 grid gap-4 sm:grid-cols-3">{stats.map((stat)=>{const Icon=stat.icon;return <div key={stat.label} className="glass-panel card-lift p-5"><div className="flex items-center justify-between"><p className="text-sm text-muted-foreground">{stat.label}</p><Icon className="size-4 text-accent-foreground"/></div><p className="mt-5 font-serif text-3xl text-primary">{stat.value}</p><p className="mt-1 text-xs text-muted-foreground">{stat.detail}</p></div>})}</div><div className="mt-8 grid gap-6 lg:grid-cols-[1.15fr_.85fr]"><section><h2 className="font-serif text-3xl text-primary">Recent searches</h2><div className="mt-4 glass-panel overflow-hidden">{["Hospital electrical wiring and emergency circuits","M30 concrete for coastal exposure","Drinking water quality for rural schools"].map((query,index)=><Link key={query} to="/search" className="grid grid-cols-[minmax(0,1fr)_auto] items-center gap-4 border-b border-border p-5 last:border-0 hover:bg-accent/40"><div className="min-w-0"><p className="truncate font-semibold text-primary">{query}</p><p className="mt-1 flex items-center gap-1 text-xs text-muted-foreground"><CalendarDays className="size-3"/> {index+2} days ago · {3+index} matches</p></div><ArrowUpRight className="size-4 text-accent-foreground"/></Link>)}</div></section><section><h2 className="font-serif text-3xl text-primary">Bookmarks</h2><div className="mt-4 grid gap-3">{standards.slice(0,3).map((standard)=><Link key={standard.id} to="/standard/$id" params={{id:standard.id}} className="glass-panel card-lift block p-4"><p className="eyebrow">{standard.number}</p><p className="mt-2 text-sm font-semibold leading-5 text-primary">{standard.title}</p></Link>)}</div></section></div></div>}